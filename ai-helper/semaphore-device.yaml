substitutions:
  device_name: ai-semaphore
  friendly_name: AI Semaphore
  ap_ssid: AP-AI-Semaphore
  ap_password: password
  time_zone: Europe/Kyiv
  wifi_rssi_update: 60s

esphome:
  name: ${device_name}
  friendly_name: ${friendly_name}
  platformio_options:
    build_flags:
      - -DAI_HELPER_SEMAPHORE_BUILD_ENABLED=1
  includes:
    - ai-helper-semaphore.h
  on_boot:
    priority: -10
    then:
      - script.execute: sem_apply_status_outputs

esp32:
  board: esp32dev
  framework:
    type: esp-idf

packages:
  common: !include ai-helper-common.yaml
  semaphore: !include ai-helper-semaphore.yaml
